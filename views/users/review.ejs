<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Review Your Order</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #fff5f5;
      font-family: 'Segoe UI', sans-serif;
      padding: 2rem;
    }

    h2 {
      color: #dc3545;
      text-align: center;
      margin-bottom: 2rem;
    }

    .review-form {
      max-width: 600px;
      margin: 0 auto;
      background: white;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(220, 53, 69, 0.15);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    label {
      display: block;
      font-weight: bold;
      margin-bottom: 0.5rem;
      color: #dc3545;
    }

    .star-select {
      width: 100%;
      padding: 0.6rem;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 1rem;
      transition: border-color 0.3s;
    }

    .star-select:hover {
      border-color: #dc3545;
    }

    .submit-btn {
      background-color: #dc3545;
      color: white;
      padding: 0.6rem 1.5rem;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      transition: background-color 0.3s;
    }

    .submit-btn:hover {
      background-color: #a52a2a;
    }
  </style>
</head>
<body>

  <h2>Review Your Order</h2>

<form action="/review/<%= order._id %>" method="POST" class="review-form">
  <% order.items.forEach(item => { %>
    <div class="form-group">
      <label><%= item.name %></label>
      <% console.log(item) %>
        <select name="ratings[<%= item.menuItemId.toString() %>]" class="star-select" required>
        <option value="">⭐ Select Rating</option>
        <% for (let i = 1; i <= 5; i++) { %>
          <option value="<%= i %>"><%= '★'.repeat(i) %> (<%= i %>)</option>
        <% } %>
      </select>
    </div>
  <% }) %>
  <div class="text-center">
    <button type="submit" class="submit-btn">Submit Review</button>
  </div>
</form>



</body>
</html>
